{% extends 'baseHF.html.twig' %}

{% block title %}Projet{% endblock %}

{% block body %}

<div style="height=500px;">

    <br>
        <h1>{{ project.name }}</h1>
    <br>
    <br>
                {#
                <tr>
                    <th>Id</th>
                    <td>{{ project.id }}</td>
                </tr>
                #}

            <h3>Type de projet :</h3><br>
            <p>{{ project.type }}</td>
    <br>
    <br>
            <h3>Description :</h3><br>
            <p>{{ project.description }}</p>

        {#  
        <a href="{{ path('app_projects_home') }}">back to list</a>

        <a href="{{ path('app_projects_edit', {'id': project.id}) }}">edit</a>

        {{ include('projects/_delete_form.html.twig') }}
        #}
</div>

{% endblock %}
