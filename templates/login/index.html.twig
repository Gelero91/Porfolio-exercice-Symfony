{% extends 'baseHF.html.twig' %}

{% block title %}Hello LoginController!{% endblock %}

{% block body %}

    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
<style>
.block {
    height:515px;
    }

    
</style>

<div class="block">
<br>
    <h1>Login</h1>

        <form action="{{ path('app_login') }}" method="post"><br>
            <label for="username" style="width:300px;">Email:</label><br>
            <input type="text" id="username" name="_username" value="{{ last_username }}">
            <br><br>
            <label for="password">Password:</label><br>
            <input type="password" id="password" name="_password">
            <br><br>

            {# If you want to control the URL the user is redirected to on success
            <input type="hidden" name="_target_path" value="/account"> #}

            <button type="submit">login</button>
        </form>
<br>
<br>
    <a class="link" href={{ path('app_register') }}>S'enregistrer</a>
    <a class="link" href={{ path('app_forgot_password_request') }}>Mot de passe oublié ?</a>
</div>
{% endblock %}